# --- MicroPython / dispositivo
*.pyc
__pycache__/
*.bin
*.uf2
*.elf

# Herramientas de flasheo / transferencia (ampy, rshell, mpremote)
.ampy_config
*.log

# Entornos virtuales (si usas Python en la PC para scripts auxiliares)
venv/
.venv/
env/
ENV/

# Editores / IDEs
.vscode/
.idea/
*.swp
*.swo
.DS_Store
Thumbs.db

# Thonny (IDE común para MicroPython)
.thonny.cfg

# Archivos de configuración local con datos sensibles
secrets.py
config_local.py
wifi_credentials.py

# Backups y temporales
*.bak
*.tmp
*~

# Navegador / dashboard
# (dashboard.html es standalone, no genera artefactos que ignorar,
#  pero por si en el futuro agregas un bundler o build local)
node_modules/
dist/
